#include<bits/stdc++.h>
using namespace std;

#ifndef MEMORY_ALLOCATION
#define MEMORY_ALLOCATION

string allocate_memory(string, string);
string increamentMemory(string, string);
string decreamentMemory(string, string);

#endif